# Nexora Backend – service dependencies
# For ML-core dependencies (model training, pipeline, etc.) see the root requirements.txt.

# ── Web Framework ──────────────────────────────────────────────────────────
# fastapi 0.115.x → starlette 0.41.x → requires httpx>=0.23,<0.28 for TestClient
fastapi==0.115.0
uvicorn[standard]==0.24.0
pydantic==2.5.0
httpx>=0.23.0,<0.28.0
requests==2.31.0

# ── Auth ───────────────────────────────────────────────────────────────────
# PyJWT for signing/verifying access tokens. Passwords are hashed with the
# stdlib `hashlib` (PBKDF2-HMAC-SHA256); no extra dependency needed there.
PyJWT>=2.7.0,<3.0.0

# ── gRPC (optional – uncomment to enable the gRPC server) ─────────────────
# grpcio>=1.59.0
# grpcio-reflection>=1.59.0
# grpcio-health-checking>=1.59.0

# ── Clinician UI ───────────────────────────────────────────────────────────
streamlit>=1.28.0

# ── Data ───────────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0

# ── Utilities ─────────────────────────────────────────────────────────────
pyyaml>=6.0.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# ── ML Logic Dependencies (required by ml_core imports in routes) ─────────
scipy>=1.10.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

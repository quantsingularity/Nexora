# ==============================================================================
# Nexora Environment Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. Core Application Settings
# ------------------------------------------------------------------------------
# A secret key for signing JSON Web Tokens (JWTs).
JWT_SECRET="your_jwt_signing_secret_here"

# Application logging level (e.g., DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL="INFO"


# ------------------------------------------------------------------------------
# 2. Database Configuration
# ------------------------------------------------------------------------------
# Full database connection URL (e.g., postgresql://user:password@host:5432/db_name)
# Used by the application to connect to the database.
DATABASE_URL="postgresql://user:password@localhost:5432/nexora_db"

# Individual database credentials (used in some deployment configurations)
DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="nexora_db"
DB_USER="nexora_user"
DB_PASSWORD="nexora_password"

# MySQL root password (used for database initialization in some deployments)
MYSQL_ROOT_PASSWORD="mysql_root_password"


# ------------------------------------------------------------------------------
# 3. External Service Integrations (FHIR Server)
# ------------------------------------------------------------------------------
# The base URL for the FHIR server API
FHIR_SERVER_URL="https://fhir.example.com/baseR4"

# Client ID for OAuth2 authentication with the FHIR server
FHIR_CLIENT_ID="your_fhir_client_id"

# Client Secret for OAuth2 authentication with the FHIR server
FHIR_CLIENT_SECRET="your_fhir_client_secret"


# ------------------------------------------------------------------------------
# 4. Model Registry Configuration
# ------------------------------------------------------------------------------
# The base URL for the Model Registry (e.g., MLflow)
MODEL_REGISTRY_URL="http://localhost:5000"

# Access Key for the Model Registry storage (e.g., S3 access key)
MODEL_REGISTRY_ACCESS_KEY="your_model_registry_access_key"

# Secret Key for the Model Registry storage (e.g., S3 secret key)
MODEL_REGISTRY_SECRET_KEY="your_model_registry_secret_key"


# ------------------------------------------------------------------------------
# 5. Service Endpoints
# ------------------------------------------------------------------------------
# The URL for the Feature Server service
FEATURE_SERVER_URL="http://localhost:8080"

# The URL for the Model Serving service
MODEL_SERVING_URL="http://localhost:8081"
